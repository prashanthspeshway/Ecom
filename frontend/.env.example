# Frontend Environment Variables
# Copy this file to .env and fill in the values

# Application name
VITE_APP_NAME=Saree Elegance

# API Base URL (leave empty for development - uses proxy in vite.config.ts)
# For production, set to your backend URL, e.g., https://api.yourdomain.com
VITE_API_BASE_URL=

# API Base path (usually /api)
VITE_API_BASE=/api

# Base URL for the app (usually / for root)
VITE_BASE_URL=/
